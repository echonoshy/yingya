# HyperFrames smoke fixture

This directory contains a deterministic HyperFrames composition used only by
Yingya's automated checks. Keep it self-contained, network-free at render time,
and compatible with the HyperFrames version pinned by the repository root.
