# 素材库改版视觉验收

- 状态：等待视觉对照
- 设计源：`/home/lake/.codex/generated_images/01a06a18-ee1a-7e51-85f7-04a0047f6b7a/exec-a14efdf6-de04-4c93-961c-e03799a7ab1e.png`
- 目标视口：1487 × 1058
- 实现地址：`http://127.0.0.1:8798/`
- 实现截图：等待生成
- 对照结论：等待将实现截图与设计源合并检查
- 已完成检查：TypeScript 类型检查、Vite 生产构建、Web 单元测试、Rust 格式检查
- 待检查：桌面同尺寸布局、390 px 窄屏、交互状态、控制台错误

当前环境没有可用的浏览器插件。根据产品设计工作流，使用仓库内 Playwright 截图工具前需要用户授权。

## 2026-09-04 单项目创作工作台改版

- final result: passed
- 用户授权：已明确允许使用 Playwright UI 测试。
- 视觉真值：`/home/lake/.codex/generated_images/01a06a8a-b5c7-7ad2-bbad-98ce64ad0105/exec-e16026d6-4d4a-402b-857f-fb6110ed5a39.png`
- 实现截图：`/tmp/yingya-ui-project-assets.png`
- 补充状态截图：`/tmp/yingya-ui-hyperframes-live.png`、`/tmp/yingya-ui-mobile.png`
- 完整对照证据：`/tmp/yingya-design-comparison-final.png`
- 局部对照证据：`/tmp/yingya-composer-comparison-final.png`（左侧普通对话、素材选择、输入区）
- 视口：桌面 1440 × 960 CSS px；移动端 360 × 800 CSS px；device scale factor 1。
- 像素与密度归一化：设计源 1486 × 1059 px；实现 1440 × 960 px。完整对照时将设计源等比缩放并居中填充到 1440 × 960；局部对照统一为 468 × 884 px。
- 状态：设计源为已生成视频与场景素材态；实现主对照为“方案待确认 + 已选择素材”态，实时预览另以 `/tmp/yingya-ui-hyperframes-live.png` 验证。动态视频内容不同，不作为结构与交互保真差异。

### 对照结论

- 信息架构：项目管理留在外层；进入项目后移除最左项目栏，只显示返回所有项目、当前项目标题和项目状态。
- 空间分配：桌面端对话区固定在可调整的紧凑宽度，预览/分镜/素材/产物工作区占据主要宽度；预览态增加创作素材检查栏。
- 创作与素材：输入框可多选全局素材，所选素材以“用于剧本与分镜”标签进入本轮创作上下文；对话上传的媒体同步沉淀为项目素材。
- 对话形态：保留标准用户/助手消息流、附件与输入框，不展示重复 Agent 头像或装饰性流程节点。
- 响应式：980 px 以下切换为对话/预览/分镜/素材/产物页签；360 px 下输入工具自动换行，无横向溢出；键盘焦点和减少动画规则保留。

### 迭代记录

1. P1：预览区在无可用实时内容时同时出现两个空状态。修正为仅在实时预览可用时挂载实时画布。
2. P1：素材选择弹层曾相对错误容器定位，可能越出视口。修正输入区定位上下文并重新截图验证。
3. P1：首轮实现缺少参考稿中的预览侧素材上下文。增加“已使用素材 / 全局素材库”检查栏及“加入提示”动作。
4. P1：360 px 输入区控件和导出图标发生挤压。修正工具栏换行、按钮尺寸和移动端显式图标。
5. P3：测试素材使用 favicon 占位，缩略图不代表生产内容；真实素材由项目媒体接口和全局素材库动态提供，不影响布局或操作验收。

### 验证

- `cargo fmt --all -- --check`：通过
- `npm run typecheck`：通过
- `npm run test:web`：12 项通过
- `npm run web:build`：通过（仅保留第三方 Zod/Rollup 注释警告）
- `cargo test --all-targets`：38 项通过
- `npm run test:ui`：通过，覆盖素材工坊、实时预览、等待输入、方案/草稿确认、失败恢复、桌面项目流程、素材选择及 360 px 创建/消息流程

## 2026-09-04 项目首页重构

- final result: passed
- 视觉真值：`/home/lake/.codex/generated_images/01a06a8a-b5c7-7ad2-bbad-98ce64ad0105/exec-6495183e-10d4-40bb-8311-635b27454822.png`
- 实现截图：`/tmp/yingya-home-desktop.png`
- 当前浏览器尺寸：`/tmp/yingya-home-browser.png`
- 响应式截图：`/tmp/yingya-home-mobile.png`、`/tmp/yingya-home-narrow.png`
- 完整对照：`/tmp/yingya-home-comparison.png`
- 视口：1440 × 960、1063 × 1180、390 × 844、320 × 800 CSS px；device scale factor 1。
- 尺寸归一化：概念稿 1536 × 1024 px 与实现 1440 × 960 px 比例均为 3:2；并排对照统一缩放到 1440 × 960。
- 页面状态：真实后端数据，4 个现有项目；全部筛选态；本地服务已连接。

### 对照检查

1. 文案：首屏只保留“视频项目 / 把想法变成可继续创作的视频项目 / 新建视频 / 今天想创作什么？ / 最近项目”及必要控件文案，和概念稿一致，无新增营销话术。
2. 布局：216 px 浅灰产品导航 + 白色主画布；创建器在上，项目管理清单在下；项目不再塞入侧栏。
3. 字体：标题、项目名、控件和元数据均使用现有 Noto Sans SC / Fragment Mono 体系，并保持明确层级。
4. 色彩：纯白主画布、冷系统灰导航、近黑主操作；系统蓝只用于筛选与焦点，状态同时使用圆点和文字。
5. 容器：创建器是唯一轻抬升区域；项目采用连续分隔清单，没有重复卡片网格或装饰性面板。
6. 图标：全部复用项目内 Phosphor 图标；映芽标志继续使用现有品牌资产。
7. 响应式：800 px 以下导航收敛为顶部栏；390/320 px 项目行隐藏次要时间和画幅，输入控件换行且无横向溢出。
8. 交互：筛选页签、项目菜单、打开项目、删除项目、新建视频聚焦、创建提交均连接真实状态和原有 API。

### 迭代记录

1. P1：桌面服务状态因隐藏图标仍占网格列而逐字换行。改为两列布局并锁定状态文案单行。
2. P1：输入框自动聚焦导致桌面内部滚动容器跳过页面标题。移除首屏强制聚焦，保留“新建视频”按钮的显式聚焦动作。
3. P3：没有项目图片素材的真实项目使用中性视频占位图标；有图片素材时自动读取项目媒体作为封面。这是数据状态差异，不影响结构保真。

### 验证

- `npm run typecheck`：通过
- `npm run test:web`：12 项通过
- `npm run web:build`：通过（仅第三方 Zod/Rollup 注释警告）
- `npm run test:ui`：通过；新增首页标题、筛选、项目菜单及打开项目回归验证
- Playwright 真实数据检查：四种视口均无横向溢出、无页面异常、无相关控制台错误
